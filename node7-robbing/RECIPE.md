# NODE7 Robbing Recipe

The recipe installs `node7-robbing` after `node7-core`, `node7-inventory`, and `node7-radialmenu`.

Required server configuration:

```cfg
ensure node7-robbing
```

No ACE permissions or SQL migration are required.
